<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ═══ 权限说明 ═══
     1. INTERNET：ffmpeg-kit 在部分平台需联网下载字体/资源（本项目本地处理，保留以防 Web/调试需要）
     2. READ/WRITE_EXTERNAL_STORAGE：仅 Android 10 (API 29) 及以下需要；
        Android 11+ 使用 SAF（file_picker 的 ACTION_OPEN_DOCUMENT）无需存储权限。
        maxSdkVersion 限制避免 Android 13+ 弹出无意义的权限请求。
     3. requestLegacyExternalStorage：Android 10 直接读写公共目录（临时文件仍建议放应用私有目录）
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>

    <application
        android:label="Subtitle Studio Pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- share_plus / 自研分享：FileProvider，供跨应用共享生成的字幕/视频文件 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>
